# PrintMatch
PrintMatch is a fingerprint comparison application that uses image processing techniques to analyze two fingerprint images and determine whether they belong to the same person. Built with Python, OpenCV, and scikit-image, it includes a simple and modern GUI.
